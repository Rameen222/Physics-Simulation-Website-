## Run with Docker
1) `cp backend/.env.example backend/.env`
2) `cp frontend/.env.example frontend/.env`
3) `docker compose up --build`

- Frontend: http://localhost:5173  
- API:      http://localhost:5000

## Endpoints
- `/api/quizzes`
- `/api/quizzes/:slug` (e.g. `pendulum`)

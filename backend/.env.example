
# -------- BACKEND (Express) ----------
PORT=5020
CORS_ORIGIN=http://localhost:5160

# -------- DATABASE (MySQL) ----------
# When running WITH Docker, the host is the compose service name "db"
# When running WITHOUT Docker, point to your local MySQL (e.g., localhost)
DB_HOST=db
DB_PORT=3307
DB_USER=app
DB_PASSWORD=app
DB_NAME=appdb
